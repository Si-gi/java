
public class Arene {
	private Monster monster;
	private Player player;
	public Arene(Monster m, Player p) {
		this.monster = m;
		this.player = p;
	}
	
	public fight() {
		while(player.getHp() > 0 || monster.getHp()) {
			player.attack(monster);
			monster.attack(player);
		}
		
	}

}
